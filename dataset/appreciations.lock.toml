# 随包赏析种子锁。**这不是种子本身**，而是指向种子的指针与摘要。
#
# 种子（`appreciations.json`）由一台装有开放权重运行时的机器生成一次，
# 上传到 `seed_tag` 这个 Release；发布流按本文件下载并校验，不在 CI 里跑推理。
# 本文件的 git 历史回答「哪组声明与字节被选中发布」，不证明正文作者来源。
#
# 由 `cargo run -p xtask -- pregenerate --endpoint <URL> --seed-tag <TAG>` 写出，
# 由 `cargo run -p xtask -- verify-seed` 校验摘要、结构、覆盖与披露一致性。
# 同一执行方可同步伪造正文与全部声明，因此校验通过不构成模型来源证明。

seed_tag = "appreciation-seed-v1"
seed_sha256 = "18aa82cd6cdf0a480f14cf216673ce864b4fc9ec15e3f49de95f676eb9ae4287"
manifest_sha256 = "af4f0e0e418651084c894bbfc3cba311c6c4bdb05434605c021528287bb7dd02"
record_count = 16
corpus_version = "0.1.0"
template_version = "1.0.0"
model = "deepseek-r1:7b"
model_license = "MIT"
provider = "ollama"
model_digest = "755ced02ce7befdb13b7ca74e1e4d08cddba4986afdb63a480f2c93d3140383f"
generated_at = 1786988342
