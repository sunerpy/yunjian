{
  "schema_version": 1,
  "platform": "linux",
  "set": "desktop",
  "date": "2026-08-14",
  "app_version": "0.1.0",
  "commit_sha": "c86b9c99db57b65458d27cb9cb4468adbe342607",
  "os_build": "Ubuntu 24.04.4 LTS / Linux 6.17.0-1019-aws",
  "session": {
    "display_kind": "virtual",
    "display": ":99",
    "window_manager": "Openbox",
    "webdriver": {
      "attempted": true,
      "succeeded": false,
      "detail": "`POST /session` 未能返回会话：POST http://127.0.0.1:4444/session 失败。本机实测：应用进程真的起来了、真实窗口映射成功、WebKit 的 inspector server 端口也在监听，但 `WebKitWebDriver` 从不向它建立连接，请求一直挂到超时。"
    },
    "audio_input_devices": 0
  },
  "all_pass": false,
  "executed_pass": 3,
  "failed": 0,
  "not_executed": 17,
  "platforms_not_executed": [
    {
      "platform": "windows",
      "reason": "本机是 Linux 容器，没有 Windows 宿主机，也没有 msedgedriver；session 0 的服务账户跑不出交互式桌面会话",
      "executable_when": "一台装有 WebView2 运行时的 Windows 11 交互式登录会话，加上与 Edge 主版本号匹配的 msedgedriver"
    },
    {
      "platform": "macos",
      "reason": "本机没有 macOS 宿主机；且 WKWebView 没有官方 WebDriver 工具，公证构建还需要签名身份",
      "executable_when": "一台交互式登录的 macOS 机器、一个 Developer ID 签名身份，以及 @wdio/tauri-service 的 embedded WebDriver（macOS 唯一可行的驱动方式）"
    }
  ],
  "assertions": [
    {
      "id": "artifact_present",
      "what": "构建产物存在且可执行，动态库依赖全部可解析",
      "channel": "process",
      "screenshot_required": false,
      "verdict": "PASS",
      "detail": "/config/workspace/ProdDir/AI/yunjian/.omo/worktrees/task-67/target/debug/yunjian-desktop 存在，ldd 未报缺失动态库",
      "executable_when": null,
      "screenshot": null
    },
    {
      "id": "installer_runs",
      "what": "安装包（.deb / NSIS / .dmg）能安装并从安装后的路径启动",
      "channel": "process",
      "screenshot_required": false,
      "verdict": "NOT EXECUTED",
      "detail": "/config/workspace/ProdDir/AI/yunjian/.omo/worktrees/task-67/target/debug/bundle 下没有安装包产物；本次验收跑的是未打包的构建产物，因此「安装后从安装路径启动」这条链没有被执行",
      "executable_when": "先 `cargo tauri build`（Linux 上还需 `dpkg-deb` 与 `librsvg2-dev`），再由 harness 安装到一个临时 root 并从该路径启动。NSIS 静默安装时**只等安装器进程本身**，不要等整棵进程树——完成页会把应用作为子进程拉起来，等进程树会永远等不完。",
      "screenshot": null
    },
    {
      "id": "app_launches",
      "what": "应用在交互式桌面会话里启动并映射出一个顶层窗口",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "PASS",
      "detail": "窗口 0x600003 已映射，`_NET_WM_NAME` = 「云笺」",
      "executable_when": null,
      "screenshot": "desktop-qa/app-launched.png"
    },
    {
      "id": "custom_titlebar_rendered",
      "what": "窗口没有窗口管理器绘制的边框（decorations: false 生效），标题栏由应用自绘",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "窗口内容不可观测：等了 20 秒，主色 #f8f8f8 占 100.0000%。窗口本身是真的（已映射、`_NET_WM_NAME` 正确、`_NET_WM_ICON` 齐备），但 X `GetImage` 读回来的是一整片单色。本机分不清两种原因——前端根本没渲染，还是 WebKit 把内容合成到了一块 X 读不到的 GL 表面（日志里有 `libEGL warning: DRI3 error: Could not get DRI3 device`，指向后者）。两种情况下点击都落在空处，因此「点了按钮之后窗口状态没变」证不了任何关于产品的事——记成 FAIL 会是一个假故障，故记未执行",
      "executable_when": "一个 WebKitGTK 会把内容真的合成到窗口上的宿主机。本机（无 GPU 的容器 + Xvfb）实测不会：`WEBKIT_DISABLE_COMPOSITING_MODE=1`、`WEBKIT_DISABLE_DMABUF_RENDERER=1`、`LIBGL_ALWAYS_SOFTWARE=1` 三个单独与组合都试过，读回来始终是单色。一台有真实显示与可用 GL 栈的 Linux 桌面登录会话即可",
      "screenshot": "desktop-qa/webview-paint.png"
    },
    {
      "id": "control_minimize_works",
      "what": "点自绘标题栏的最小化按钮，窗口真的最小化",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "窗口内容不可观测：等了 20 秒，主色 #f8f8f8 占 100.0000%。窗口本身是真的（已映射、`_NET_WM_NAME` 正确、`_NET_WM_ICON` 齐备），但 X `GetImage` 读回来的是一整片单色。本机分不清两种原因——前端根本没渲染，还是 WebKit 把内容合成到了一块 X 读不到的 GL 表面（日志里有 `libEGL warning: DRI3 error: Could not get DRI3 device`，指向后者）。两种情况下点击都落在空处，因此「点了按钮之后窗口状态没变」证不了任何关于产品的事——记成 FAIL 会是一个假故障，故记未执行",
      "executable_when": "一个 WebKitGTK 会把内容真的合成到窗口上的宿主机。本机（无 GPU 的容器 + Xvfb）实测不会：`WEBKIT_DISABLE_COMPOSITING_MODE=1`、`WEBKIT_DISABLE_DMABUF_RENDERER=1`、`LIBGL_ALWAYS_SOFTWARE=1` 三个单独与组合都试过，读回来始终是单色。一台有真实显示与可用 GL 栈的 Linux 桌面登录会话即可",
      "screenshot": "desktop-qa/webview-paint.png"
    },
    {
      "id": "control_maximize_works",
      "what": "点自绘标题栏的最大化按钮，窗口真的最大化",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "窗口内容不可观测：等了 20 秒，主色 #f8f8f8 占 100.0000%。窗口本身是真的（已映射、`_NET_WM_NAME` 正确、`_NET_WM_ICON` 齐备），但 X `GetImage` 读回来的是一整片单色。本机分不清两种原因——前端根本没渲染，还是 WebKit 把内容合成到了一块 X 读不到的 GL 表面（日志里有 `libEGL warning: DRI3 error: Could not get DRI3 device`，指向后者）。两种情况下点击都落在空处，因此「点了按钮之后窗口状态没变」证不了任何关于产品的事——记成 FAIL 会是一个假故障，故记未执行",
      "executable_when": "一个 WebKitGTK 会把内容真的合成到窗口上的宿主机。本机（无 GPU 的容器 + Xvfb）实测不会：`WEBKIT_DISABLE_COMPOSITING_MODE=1`、`WEBKIT_DISABLE_DMABUF_RENDERER=1`、`LIBGL_ALWAYS_SOFTWARE=1` 三个单独与组合都试过，读回来始终是单色。一台有真实显示与可用 GL 栈的 Linux 桌面登录会话即可",
      "screenshot": "desktop-qa/webview-paint.png"
    },
    {
      "id": "control_restore_works",
      "what": "最大化后再点同一个按钮，窗口真的还原",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "窗口内容不可观测：等了 20 秒，主色 #f8f8f8 占 100.0000%。窗口本身是真的（已映射、`_NET_WM_NAME` 正确、`_NET_WM_ICON` 齐备），但 X `GetImage` 读回来的是一整片单色。本机分不清两种原因——前端根本没渲染，还是 WebKit 把内容合成到了一块 X 读不到的 GL 表面（日志里有 `libEGL warning: DRI3 error: Could not get DRI3 device`，指向后者）。两种情况下点击都落在空处，因此「点了按钮之后窗口状态没变」证不了任何关于产品的事——记成 FAIL 会是一个假故障，故记未执行",
      "executable_when": "一个 WebKitGTK 会把内容真的合成到窗口上的宿主机。本机（无 GPU 的容器 + Xvfb）实测不会：`WEBKIT_DISABLE_COMPOSITING_MODE=1`、`WEBKIT_DISABLE_DMABUF_RENDERER=1`、`LIBGL_ALWAYS_SOFTWARE=1` 三个单独与组合都试过，读回来始终是单色。一台有真实显示与可用 GL 栈的 Linux 桌面登录会话即可",
      "screenshot": "desktop-qa/webview-paint.png"
    },
    {
      "id": "control_close_works",
      "what": "点自绘标题栏的关闭按钮，窗口关闭且进程退出",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "窗口内容不可观测：等了 20 秒，主色 #f8f8f8 占 100.0000%。窗口本身是真的（已映射、`_NET_WM_NAME` 正确、`_NET_WM_ICON` 齐备），但 X `GetImage` 读回来的是一整片单色。本机分不清两种原因——前端根本没渲染，还是 WebKit 把内容合成到了一块 X 读不到的 GL 表面（日志里有 `libEGL warning: DRI3 error: Could not get DRI3 device`，指向后者）。两种情况下点击都落在空处，因此「点了按钮之后窗口状态没变」证不了任何关于产品的事——记成 FAIL 会是一个假故障，故记未执行",
      "executable_when": "一个 WebKitGTK 会把内容真的合成到窗口上的宿主机。本机（无 GPU 的容器 + Xvfb）实测不会：`WEBKIT_DISABLE_COMPOSITING_MODE=1`、`WEBKIT_DISABLE_DMABUF_RENDERER=1`、`LIBGL_ALWAYS_SOFTWARE=1` 三个单独与组合都试过，读回来始终是单色。一台有真实显示与可用 GL 栈的 Linux 桌面登录会话即可",
      "screenshot": "desktop-qa/webview-paint.png"
    },
    {
      "id": "double_click_maximizes_exactly_once",
      "what": "双击标题栏恰好最大化一次（自己再挂一个双击处理器会双切换回原样）",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "窗口内容不可观测：等了 20 秒，主色 #f8f8f8 占 100.0000%。窗口本身是真的（已映射、`_NET_WM_NAME` 正确、`_NET_WM_ICON` 齐备），但 X `GetImage` 读回来的是一整片单色。本机分不清两种原因——前端根本没渲染，还是 WebKit 把内容合成到了一块 X 读不到的 GL 表面（日志里有 `libEGL warning: DRI3 error: Could not get DRI3 device`，指向后者）。两种情况下点击都落在空处，因此「点了按钮之后窗口状态没变」证不了任何关于产品的事——记成 FAIL 会是一个假故障，故记未执行",
      "executable_when": "一个 WebKitGTK 会把内容真的合成到窗口上的宿主机。本机（无 GPU 的容器 + Xvfb）实测不会：`WEBKIT_DISABLE_COMPOSITING_MODE=1`、`WEBKIT_DISABLE_DMABUF_RENDERER=1`、`LIBGL_ALWAYS_SOFTWARE=1` 三个单独与组合都试过，读回来始终是单色。一台有真实显示与可用 GL 栈的 Linux 桌面登录会话即可",
      "screenshot": "desktop-qa/webview-paint.png"
    },
    {
      "id": "drag_from_title_text",
      "what": "按住标题文字本身拖动，窗口位置真的改变（data-tauri-drag-region=\"deep\"）",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "窗口内容不可观测：等了 20 秒，主色 #f8f8f8 占 100.0000%。窗口本身是真的（已映射、`_NET_WM_NAME` 正确、`_NET_WM_ICON` 齐备），但 X `GetImage` 读回来的是一整片单色。本机分不清两种原因——前端根本没渲染，还是 WebKit 把内容合成到了一块 X 读不到的 GL 表面（日志里有 `libEGL warning: DRI3 error: Could not get DRI3 device`，指向后者）。两种情况下点击都落在空处，因此「点了按钮之后窗口状态没变」证不了任何关于产品的事——记成 FAIL 会是一个假故障，故记未执行",
      "executable_when": "一个 WebKitGTK 会把内容真的合成到窗口上的宿主机。本机（无 GPU 的容器 + Xvfb）实测不会：`WEBKIT_DISABLE_COMPOSITING_MODE=1`、`WEBKIT_DISABLE_DMABUF_RENDERER=1`、`LIBGL_ALWAYS_SOFTWARE=1` 三个单独与组合都试过，读回来始终是单色。一台有真实显示与可用 GL 栈的 Linux 桌面登录会话即可",
      "screenshot": "desktop-qa/webview-paint.png"
    },
    {
      "id": "taskbar_icon_correct",
      "what": "窗口带 _NET_WM_ICON，任务栏能取到图标",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "PASS",
      "detail": "`_NET_WM_ICON` 有 2306 个 32 位字（宽高 2 字 + 逐像素 ARGB），任务栏与 alt-tab 能取到图标",
      "executable_when": null,
      "screenshot": "desktop-qa/taskbar-icon.png"
    },
    {
      "id": "tray_icon_correct",
      "what": "托盘图标存在且背景透明",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "应用尚未创建托盘图标（`yunjian-app` 里没有 tray 接线），因此没有可观测的托盘项。图标资产本身的透明度由 `xtask verify-icons` 逐字节守着",
      "executable_when": "在应用里接入托盘后，由 StatusNotifier 侧观测托盘项",
      "screenshot": null
    },
    {
      "id": "ime_prefilled_search_box",
      "what": "中文输入法往一个**已有内容**的检索框里输入：不冻结且字符落入框内（tauri#15436）",
      "channel": "web_driver",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "WebDriver 会话未能建立，DOM 层事实无法观测；刻意不用 mock 或 stub 顶替（那会把真机验收退化成单元测试）",
      "executable_when": "一个 WebKitGTK 的自动化握手可用、且 WebView 真的会绘制的宿主机。本机（Ubuntu 24.04 容器 + WebKitGTK 2.52.3 + Xvfb，无 GPU）实测：应用进程起来了、真实窗口映射成功、`TAURI_WEBVIEW_AUTOMATION=true` 已透传、WebKit 的 inspector server 端口也在监听，但 `POST /session` 一直挂到超时；同一环境下 WebView 一个像素也没画（见 `webview_paints` 一节），两件事同源。或改用 `@wdio/tauri-service` 的 embedded WebDriver 方案。",
      "screenshot": null
    },
    {
      "id": "ime_prefilled_search_box_no_freeze",
      "what": "承 tauri#15436：聚焦已有内容的检索框并输入后，界面仍然响应（OS 层可证的那一半）",
      "channel": "os_harness",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "窗口内容不可观测：等了 20 秒，主色 #f8f8f8 占 100.0000%。窗口本身是真的（已映射、`_NET_WM_NAME` 正确、`_NET_WM_ICON` 齐备），但 X `GetImage` 读回来的是一整片单色。本机分不清两种原因——前端根本没渲染，还是 WebKit 把内容合成到了一块 X 读不到的 GL 表面（日志里有 `libEGL warning: DRI3 error: Could not get DRI3 device`，指向后者）。两种情况下点击都落在空处，因此「点了按钮之后窗口状态没变」证不了任何关于产品的事——记成 FAIL 会是一个假故障，故记未执行",
      "executable_when": "一个 WebKitGTK 会把内容真的合成到窗口上的宿主机。本机（无 GPU 的容器 + Xvfb）实测不会：`WEBKIT_DISABLE_COMPOSITING_MODE=1`、`WEBKIT_DISABLE_DMABUF_RENDERER=1`、`LIBGL_ALWAYS_SOFTWARE=1` 三个单独与组合都试过，读回来始终是单色。一台有真实显示与可用 GL 栈的 Linux 桌面登录会话即可",
      "screenshot": "desktop-qa/webview-paint.png"
    },
    {
      "id": "two_char_search_returns_results",
      "what": "两字检索（明月）返回结果行",
      "channel": "web_driver",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "WebDriver 会话未能建立，DOM 层事实无法观测；刻意不用 mock 或 stub 顶替（那会把真机验收退化成单元测试）",
      "executable_when": "一个 WebKitGTK 的自动化握手可用、且 WebView 真的会绘制的宿主机。本机（Ubuntu 24.04 容器 + WebKitGTK 2.52.3 + Xvfb，无 GPU）实测：应用进程起来了、真实窗口映射成功、`TAURI_WEBVIEW_AUTOMATION=true` 已透传、WebKit 的 inspector server 端口也在监听，但 `POST /session` 一直挂到超时；同一环境下 WebView 一个像素也没画（见 `webview_paints` 一节），两件事同源。或改用 `@wdio/tauri-service` 的 embedded WebDriver 方案。",
      "screenshot": null
    },
    {
      "id": "corpus_first_run_materialization",
      "what": "首启语料物化完成并显示进度",
      "channel": "web_driver",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "WebDriver 会话未能建立，DOM 层事实无法观测；刻意不用 mock 或 stub 顶替（那会把真机验收退化成单元测试）",
      "executable_when": "一个 WebKitGTK 的自动化握手可用、且 WebView 真的会绘制的宿主机。本机（Ubuntu 24.04 容器 + WebKitGTK 2.52.3 + Xvfb，无 GPU）实测：应用进程起来了、真实窗口映射成功、`TAURI_WEBVIEW_AUTOMATION=true` 已透传、WebKit 的 inspector server 端口也在监听，但 `POST /session` 一直挂到超时；同一环境下 WebView 一个像素也没画（见 `webview_paints` 一节），两件事同源。或改用 `@wdio/tauri-service` 的 embedded WebDriver 方案。",
      "screenshot": null
    },
    {
      "id": "shipped_appreciation_without_key",
      "what": "没有 API key 时随包赏析仍能渲染，且带「AI 赏析」标签与未审校说明",
      "channel": "web_driver",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "WebDriver 会话未能建立，DOM 层事实无法观测；刻意不用 mock 或 stub 顶替（那会把真机验收退化成单元测试）",
      "executable_when": "一个 WebKitGTK 的自动化握手可用、且 WebView 真的会绘制的宿主机。本机（Ubuntu 24.04 容器 + WebKitGTK 2.52.3 + Xvfb，无 GPU）实测：应用进程起来了、真实窗口映射成功、`TAURI_WEBVIEW_AUTOMATION=true` 已透传、WebKit 的 inspector server 端口也在监听，但 `POST /session` 一直挂到超时；同一环境下 WebView 一个像素也没画（见 `webview_paints` 一节），两件事同源。或改用 `@wdio/tauri-service` 的 embedded WebDriver 方案。",
      "screenshot": null
    },
    {
      "id": "voice_round_succeeds_end_to_end",
      "what": "语音背诵一轮端到端成功：采集 -> ASR -> 无偏置评分这条链跑通",
      "channel": "web_driver",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "WebDriver 会话未能建立，DOM 层事实无法观测；刻意不用 mock 或 stub 顶替（那会把真机验收退化成单元测试）",
      "executable_when": "一个 WebKitGTK 的自动化握手可用、且 WebView 真的会绘制的宿主机。本机（Ubuntu 24.04 容器 + WebKitGTK 2.52.3 + Xvfb，无 GPU）实测：应用进程起来了、真实窗口映射成功、`TAURI_WEBVIEW_AUTOMATION=true` 已透传、WebKit 的 inspector server 端口也在监听，但 `POST /session` 一直挂到超时；同一环境下 WebView 一个像素也没画（见 `webview_paints` 一节），两件事同源。或改用 `@wdio/tauri-service` 的 embedded WebDriver 方案。",
      "screenshot": null
    },
    {
      "id": "voice_degradation_states_reason",
      "what": "失败路径单独验证：语音不可用时切到打字模式并显示具体原因",
      "channel": "web_driver",
      "screenshot_required": true,
      "verdict": "NOT EXECUTED",
      "detail": "WebDriver 会话未能建立，DOM 层事实无法观测；刻意不用 mock 或 stub 顶替（那会把真机验收退化成单元测试）",
      "executable_when": "一个 WebKitGTK 的自动化握手可用、且 WebView 真的会绘制的宿主机。本机（Ubuntu 24.04 容器 + WebKitGTK 2.52.3 + Xvfb，无 GPU）实测：应用进程起来了、真实窗口映射成功、`TAURI_WEBVIEW_AUTOMATION=true` 已透传、WebKit 的 inspector server 端口也在监听，但 `POST /session` 一直挂到超时；同一环境下 WebView 一个像素也没画（见 `webview_paints` 一节），两件事同源。或改用 `@wdio/tauri-service` 的 embedded WebDriver 方案。",
      "screenshot": null
    },
    {
      "id": "app_exits_cleanly",
      "what": "应用正常退出，退出码为 0，不留孤儿进程",
      "channel": "process",
      "screenshot_required": false,
      "verdict": "NOT EXECUTED",
      "detail": "退出这条以「点关闭按钮」为入口，而按钮没有被画出来，所以这条链没有被走过",
      "executable_when": "同上：一个 WebView 会绘制的宿主机",
      "screenshot": null
    }
  ]
}
