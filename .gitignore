# Rust 构建产物
/target
**/*.rs.bk

# 环境与密钥：密钥只走系统钥匙串或环境变量，绝不入库
.env
.env.*
!.env.example

# 语料构建产物：由 xtask 从公共领域源重新生成，不入库
/corpus/build/
*.db
*.db-wal
*.db-shm
*.db.gz

# 模型缓存：仅收录 MIT / Apache-2.0 权重，且体积过大不入库
/models/cache/

# `xtask cer-spike --force-cer` 的自检产物。它刻意不是测量结果，不该被误读成结论，
# 更不该进版本库与下游 todo 的视野。
/docs/reports/asr-cer.selfcheck.*

# 前端依赖：由 `npm ci` 按 package-lock.json 还原，不入库
/app/node_modules/

# 前端构建产物。它同时是 cargo 的编译期前置条件——`generate_context!` 在
# `build.frontendDist` 指向的目录缺失时直接 panic——但**不能靠往库里放一个 .gitkeep
# 占位来满足**：`vite build` 会先清空 outDir，占位文件在第一次构建后就消失（已实测）。
# 因此产物不入库，改由 Makefile 的 `$(FRONTEND_DIST)` 目标在缺失时自动构建。
/app/dist/

# Tauri 移动端脚手架生成物（todo 68/69 才会出现）
/crates/yunjian-app/gen/

# 编辑器与操作系统噪声
.DS_Store
Thumbs.db

# 编排工具的本地状态：计划、笔记、会话记录与验收日志，不属于产品代码，
# 且仓库为公开仓库，不应外泄内部过程记录
/.omo/
