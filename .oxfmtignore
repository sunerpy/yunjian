# oxfmt 的忽略清单。
#
# 必须经 `--ignore-path .oxfmtignore` 显式传入：oxfmt 默认只读 `.gitignore` 与
# `.prettierignore`，**不会自动发现本文件**。`make fmt` / `make fmt-check` 已经
# 同时传入本文件与 `.gitignore`，请通过 make 调用，不要裸跑 `oxfmt`。

# ---- 工具自有的产物：人改就会和生成方打架 ----
# 这些文件由 release-please 与 git-cliff 生成并持有。重新格式化会产生假 diff，
# 更糟的是会让发布 PR 上的 fmt-check 无法变绿——而发布 PR 恰恰是必须绿的那一个。
changelog/
CHANGELOG.md
.release-please-manifest.json
release-please-config.json

# ---- 逐字节受保护：上游 LICENSE 原文 ----
# `corpus/licenses/` 下是上游仓库 LICENSE 的原样副本，其 SHA-256 由
# `cargo run -p xtask -- verify-sources` 校验，改动一个字节即构建失败。
# 当前 oxfmt 不认识 `.LICENSE` 扩展名，因此这条是纵深防御：一旦有人在此目录下
# 放入 `.md` 之类可识别的文件，它就是唯一的拦阻。
corpus/licenses/
# `models/licenses/` 同理，而且更硬：那里存的是**按摘要校验**的许可证据，含 HuggingFace
# 模型卡（`.CARD.md`）。oxfmt 会重排 Markdown，一个字节都不能动——实测就撞过：
# `make fmt` 改了 `.CARD.md` 之后 `verify-models` 立刻报「许可证据摘要不符」。
models/licenses/
# 顶层 `licenses/` 是分发用的署名副本，与 `models/licenses/` 必须逐字节相同（有测试断言）。
# 它同样含 `.CARD.md`，改一个字节两处就不再相同。**只挡许可原文，不挡 README.md**——
# 那份是人工维护的说明文档，仍受格式化管辖。
licenses/*.LICENSE
licenses/*.CARD.md

# ---- 生成物与本地状态 ----
# 语料构建产物由 xtask 从公共领域源重新生成，不属于人工维护的文本。
corpus/build/
# `corpus/reports/*.md` 由 xtask 生成（`index-spike`、`corpus-quality`、`corpus-measure`）。
# oxfmt 会按 CJK 宽度重排表格，于是「跑一次 xtask」与「跑一次 make fmt」互相覆盖，
# fmt-check 在两者之间永远有一次是红的。与 changelog/ 同理：生成方持有该文件。
corpus/reports/*.md
# 生成的 `.json` 同理，理由是实测出来的：oxfmt 会把**够短的**单元素数组折成一行，
# 而 `serde_json::to_string_pretty` 一律展开，两者互相覆盖。
# `index-mode.json` 当前是绿的纯属偶然——它的 `explain_query_plan` 字符串够长
# （该缩进深度下折起来约 97 列），恰好超过 oxfmt 的折行阈值所以保持展开；
# `measurements.json` 的同名字段短一些就被折了。也就是说「生成的 JSON 本就是规范
# 形态」这个说法是错的，它只在字符串恰好够长时成立。生成方持有这两个文件。
corpus/reports/*.json
# `docs/reports/*` 与 CER 的 fixture 同理，生成方是 `xtask cer-spike`：
# 报告的 Markdown 含 CJK 表格，JSON 由 `serde_json::to_string_pretty` 写出，
# `cer-poems.toml` 由 `toml::to_string_pretty` 写出，逐句转写是 JSONL（oxfmt 会试图
# 把它当成单个 JSON 文档）。四者都会与 oxfmt 互相覆盖，因此一律归生成方持有。
# `asr-cer-human.md` 是**人工维护**的占位文档，不在此列，仍然受格式化管辖。
docs/reports/asr-cer.md
docs/reports/asr-cer.json
docs/reports/asr-cer-transcripts.jsonl
crates/yunjian-voice/tests/fixtures/cer-poems.toml
target/
.omo/
