{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "桌面端基础能力。窗口控制权限由 todo 60 的自绘标题栏按最小集补齐——注意 core:default 已含 allow-internal-toggle-maximize（注入脚本的双击用它），但它与按钮需要的 allow-toggle-maximize 是两条不同的命令，缺哪一条都是静默失败。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
